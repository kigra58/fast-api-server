apiVersion: v1
kind: Service
metadata:
  name: fast-api-server-service
spec:
  type: NodePort
  selector:
    app: fast-api-server
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
